{
  "extends": "../tsconfig.json",

  "exclude": [
    "node_modules",
  ],

  "compilerOptions": {

    /* list directory options first */
    "baseUrl": ".",
    "outDir": "./dist/out-tsc",
    "rootDir": ".",

    /* compiles to this JS version - higher for better debug support, lower for wider browser support */
    "target": "es5",

    /* for browser-side scripts - extra types which you know browser will support */
    "lib": [
      "es2018",
      "dom"
    ],

    /* module loader format (independent of target) - es2015 for angular */
    "module": "es2015",

    /* workaround for core.js - see https://github.com/angular/angular-cli/issues/13954 */
    "paths": {
      "core-js/es7/reflect": [
        "node_modules/core-js/proposals/reflect-metadata"
      ],
      "core-js/es6/*": [
        "node_modules/core-js/es/*"
      ]
    }
  }
}
