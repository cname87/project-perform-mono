import express from 'express';
/**
 * Sets up express middleware and responds to incoming requests.
 * @param app
 * The express app object
 * app.locals holds other set up objects including the array used to store
 * the https(s) servers.
 * @returns
 * Void.
 */
export declare function runServer(app: express.Application): Promise<void>;
//# sourceMappingURL=runServer.d.ts.map
